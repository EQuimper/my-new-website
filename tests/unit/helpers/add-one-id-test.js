import { addOneId } from 'my-app/helpers/add-one-id';
import { module, test } from 'qunit';

module('Unit | Helper | add one id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addOneId([42]);
  assert.ok(result);
});

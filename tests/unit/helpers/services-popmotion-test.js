import { servicesPopmotion } from 'my-app/helpers/services-popmotion';
import { module, test } from 'qunit';

module('Unit | Helper | services popmotion');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = servicesPopmotion([42]);
  assert.ok(result);
});
